<h1>Listing Coffee Types</h1>

<%= link_to 'New Coffee Type', new_admin_coffee_type_path %>

<table>
  <!-- <tr>
    <th>Name</th>
    <th>Slug</th>
    <th>Description</th>
    <th>User</th>
    <th>Is public</th>
    <th>Is codebits</th>
    <th></th>
    <th></th>
    <th></th>
  </tr> -->

<% @coffee_types.each do |coffee_type| %>
  <tr>
    <td><%= coffee_type.brand.name %></td>
    <td><%= coffee_type.name %></td>
    
    <td><%= link_to 'Show', admin_coffee_type_path(coffee_type) %></td>
    <td><%= link_to 'Edit', edit_admin_coffee_type_path(coffee_type) %></td>
    <td><%= link_to 'Destroy', admin_coffee_type_path(coffee_type), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />


